# Rawaj needs NO environment variables.
#
# The AI Opportunity Report uses built-in, Al Qua'a-specific data and returns
# instantly — no API key, works fully offline and on Vercel with zero config.
#
# This file is intentionally left empty.
